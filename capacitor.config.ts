import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.serialmapper.app',
  appName: 'serialmapper-prisma',
  webDir: 'build',
};

export default config;
